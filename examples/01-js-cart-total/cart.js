/**
 * Считает итоговую сумму корзины.
 *
 * items — массив { price: number, qty: number }.
 * Если сумма товаров от 5000 и выше, действует скидка 10%.
 * Результат округляется до копеек (2 знака после запятой).
 */
export function cartTotal(items) {
  // TODO: реализуйте функцию
}
