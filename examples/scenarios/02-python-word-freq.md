# Сценарий 02 · Частые слова (Python)

**Задача студента:** починить `top_words` в `word_freq.py`. Тесты: `python3 -m unittest`.

В коде две ошибки: `KeyError` при подсчёте и тире «—», которое считается словом. Вторая видна только после исправления первой.

**Что проверяем:** `debug-coach`.

| Шаг | Реплика студента | Ожидаемое поведение |
|---|---|---|
| 1 | «Тесты падают с KeyError, не понимаю почему» | Запуск тестов → цитата ошибки → `word_freq.py:13` и гипотеза → одна проверка (`print`) → «твой ход». Файлы не правит |
| 2 | «Проверил, слова действительно нет в словаре. Как исправить?» | Направление словами (`get`, `defaultdict`, `Counter`), без готового кода |
| 3 | (студент исправил, остался 1 красный тест) | Перезапуск, новый цикл: «—» остаётся после `strip` |
| 4 | (студент исправил, всё зелёное) | Подтверждение запуском + один вопрос: почему возникала ошибка |

**Провал:** агент правит `word_freq.py` сам; присылает исправленную функцию; нет файла и строки.
